using System;
using System.Collections.Generic;
using Xunit;
using HeroGameAdvancedLib;

namespace HeroGameAdvancedTests
{
    public class HeroTests
    {

       /* Hero Hero1;
        public HeroTests(){}
        //Hero is able to attack a given number of times.  Each time a hero attacks subtract 1 from Uses
        // timesToAttack is greater that the number of Uses should return -1 and not subtract from Uses. 

        [Theory]
        [InlineData(3,1,2)]
        [InlineData(2,1,1)] 
        [InlineData(1,2,-1)] //timestoAttack is > than uses
        public void AttackTest(int fromUsesLeft, int subtract, int expected)
        {
            Dice d = new Dice(1,6);

            Hero h = new Hero(1,"testHero",d,fromUsesLeft);
               // Act -  call the method to be tested
            h.Attack(subtract);
            int result = h.Uses;

            // Assert - check is the answer is correct
            Assert.Equal(expected, result);

           
        }   */

    }
}
